import mysql.connector as con
import datetime
#CREATE TABLE charges (id INT PRIMARY KEY AUTO_INCREMENT,
#transaction_id INT NOT NULL,
#user_id_charge_affected INT NOT NULL,
#amount DECIMAL(2) NOT NULL,
#created_by INT NOT NULL,
#created_at DATE NOT NULL,
#modified_by INT,
#modified_at DATE,
#FOREIGN KEY (transaction_id) REFERENCES transactions(id),
#FOREIGN KEY (user_id_charge_affected) REFERENCES persons(id),
#FOREIGN KEY (created_by) REFERENCES persons(id),
#FOREIGN KEY (modified_by) REFERENCES persons(id)
#);

